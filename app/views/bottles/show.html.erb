
<h1><%= @bottle.name %></h1>

<% user = User.find_by_id(session["user_id"]) %>
<% if session["user_id"].present? && user.user_type == "admin"%>
      <table>
           <tr>
            <td><%= link_to 'Edit this bottle', edit_bottle_url(@bottle), class: 'btn' %></td>
          </tr>
      </table>
<% end %>

<table width=650px style = "padding=10px 10px 10px 10px;">
<tr>
<td width=300px>
        <%= image_tag("#{@bottle.picture_source}") %>
</td>
<td>
      <p>
        <b>Name:</b>
        <%= @bottle.name %>
      </p>

      <p>
        <b>Brand:</b>
        <%= @bottle.brand.name %>
      </p>

      <p>
        <b>Year:</b>
        <%= @bottle.year %>
      </p>

      <p>
        <b>Color:</b>
        <%= @bottle.color %>
      </p>
      
      <p>
        <b>Varietal:</b>
        <ul>
          <% @bottle.varietalassignments.each do |o| %>
             <li><%=  o.varietal.name %></li>
        <% end %>
        </ul>
      </p>
      
      <p>
        <b>Appellation:</b>
        <%= @bottle.appellation %>
      </p>

      <p>
        <b>Style:</b>
        <%= @bottle.style %>
      </p>

      <p>
        <b>
          <% if @bottle.flavorassignments.count > 1 %>
          <%= "Flavor".pluralize + ":" %>
          <% else %>
          Flavor:
          <%end%></b>
        <ul>
          <% @bottle.flavorassignments.each do |o| %>
             <li><%=  o.flavor.name %></li>
        <% end %>
        </ul>
      </p>  


      <p>
        <b>Alcohol:</b>
        <%= @bottle.alcohol %>
      </p>

      <p>
        <b>Production:</b>
        <%= @bottle.production %>
      </p>

      <p>
        <b>Price:</b>
        <%= number_to_currency(@bottle.price) %>
      </p>

      <p>
        <b>Description:</b>
        <%= @bottle.description %>
      </p>

      <p>
        <b><% if @bottle.occasions.count > 1 %>
          <%= "Occasion".pluralize + ":" %>
          <% else %>
          Occasion:
          <%end%></b>
        <ul>
          <% @bottle.occasions.each do |o| %>
             <li><%=  o.event.event %></li>
        <% end %>
        </ul>
      </p>
</td>
<tr>
</table>
